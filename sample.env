APP_PORT=
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
TIMEZONE=
ENVIRONMENT=
SECRET_JWT=
JWT_EXPIRATION_HOURS=

SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_BUCKET=


DATABASE_URL=

HTTPONLY=
SECURE=
ENVIRONMENT=

APP_HOST=

ALLOW_ORIGIN=

# Better Auth is mounted at /api/v1/auth. Use a cryptographically random value.
BETTER_AUTH_URL=http://localhost:8000/api/v1/auth
BETTER_AUTH_SECRET=

# Better Auth validates its reset token, then redirects the browser here with
# `?token=<token>`. This origin is added to Better Auth trusted origins.
FRONTEND_RESET_PASSWORD_URL=http://localhost:3000/reset-password

# SMTP transport for password reset emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Public image URLs used by src/template/reset-password.html.
PASSWORD_RESET_LOGO_URL=
PASSWORD_RESET_ILLUSTRATION_URL=
